# Storekriti Leads API (local)

This is a tiny Express API to receive landing-page form submissions.

## Run locally

```bash
cd server
npm install
npm run dev
```

Health check:
- http://localhost:5050/api/health

Leads are stored in:
- server/data/leads.jsonl
